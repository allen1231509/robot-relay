{
  "name": "robot-relay-server",
  "version": "1.0.0",
  "description": "WebSocket relay server for Nova robot remote control",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.16.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
